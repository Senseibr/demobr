---
- hosts: all
  remote_user: centos
  
  tasks:
    
    ###################
    # Time management
    ###################
    
    - name: Install NTP
      yum:
        name:
          - ntp 
          - vim
          - htop

    - name: Start the ntp service
      service: name=ntp state=started enabled=true

    
